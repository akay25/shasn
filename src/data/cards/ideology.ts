import type { IdeologyCard } from "@/engine/types";

// PLACEHOLDER: the data agent replaces this with ~20 stub cards (5 per
// Ideologue) with prompt text and varied resource payouts. At least 2 should
// have `advisory: "mature" | "trigger"` so the Content Advisory toggle is
// exercised. Schema stays the same; only the array contents change.
export const IDEOLOGY_CARDS: IdeologyCard[] = [];
